/**
 * O nome da turma é lido igual dos dois lados — o servidor guarda o turno que
 * tira dele, a tela mostra o nome curto — então as regras moram em `shared/`.
 */
export { nomeCurtoDaTurma, turnoDaTurma, type Turno } from '../../shared/turma';
